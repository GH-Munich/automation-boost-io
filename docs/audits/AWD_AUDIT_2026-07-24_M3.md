# AWD-Audit — M3 (Türen A/B/C + Lead-Layer Welle 1)

Datum: 2026-07-24 · Auslöser: Abschluss des M3-Frontends vor weiterer Umsetzung ·
Verfahren: `docs/AWD_REVIEW_STANDARD.md` (Mehr-Augen-Prinzip) ·
Stand des Codes: Branch `claude/m2-rule-engine-golden-tests-4wqc48`, bis Commit `3d85e0d`.

## Umfang & Brillen

Geprüft: Regel-Engine (`apps/detektor/src/engine/**`), App (`apps/detektor/app/**`),
Content-JSONs, Konfiguration, Lockfile, Git-Historie — gegen `docs/AWD_KONZEPT_v1.0.md`
und `CLAUDE.md`. Acht unabhängige, adversariale, nur-lesende Brillen:
Fachlogik · Determinismus · Compliance/Begründung · Hardcoding · UI-Treue ·
Red-Team · Usability (stärkstes Modell) · Security (stärkstes Modell).
Alle Befunde wurden gegen den Code verifiziert; die meisten sind durch mehrere
Brillen unabhängig bestätigt.

## Fundament (bestätigt tragfähig)

Score-Arithmetik/Bänder ohne Off-by-one · Golden-Referenzfall bitgenau ·
TCO-Rechenwege korrekt · kein Zeit/Zufall/LLM/Netz im Bewertungspfad ·
Shared Core auch im Client · keine Secrets (gesamte Historie) · kein Tracking/Egress ·
Server/Client-Grenze sauber (`server-only`) · kein Pfad-Traversal ·
TCO-Formel-Interpreter statt `eval` · AI-Act-Disclaimer wörtlich · G5-Bandbreite korrekt ·
CI (Golden-Tests + Typecheck) als Release-Gate.

Der Bewertungskern ist reproduzierbar und im Kern verteidigbar. Die Befunde betreffen
Härtung, Compliance-Sichtbarkeit, UX und Robustheit — keine kaputte Engine.

---

## Topf A — Code-Fixes (verifiziert; Umsetzung nach Freigabe)

### A1 · Ergebnis-Integrität (P0)
- **[HOCH, BESTÄTIGT]** `pricing.ts` — Washing-Faktor: Ampel entscheidet auf ungerundetem Wert, angezeigt wird der gerundete → gleiche Zahl „10,0×" kann gelb ODER rot sein (54.000 € vs. 54.200 €). Fix: Anzeige und Ampel aus demselben Wert ableiten.
- **[HOCH, BESTÄTIGT]** `PreisCheck.tsx` — Angebot 0 → „0,0× grün"; Volumen 0 → „∞× Agent Washing" (÷0). Fix: neutraler Leerzustand, Volumen `min=1`.
- **[HOCH, BESTÄTIGT]** `Assessment.tsx` — vorläufiger Score defaultet auf die höchste Klasse → **unterzeichnet** den Washing-Faktor (alles „weiß nicht" → grün 0,7× statt ggf. rot). Fix: Washing-Faktor als Spanne über die plausiblen Klassen; nicht auf eine Seite ankern. *(Richtungsentscheid siehe B2)*
- **[MITTEL, BESTÄTIGT]** `muster.ts` — Substring-Treffer („bot" in „Roboter/Voicebot", „rag" in „Fragen") → falsches Top-Muster. Fix: Wortgrenzen-Match + negative Golden-Tests.

### A2 · Compliance-Sichtbarkeit (P0; nur vorhandene JSON-Texte wörtlich rendern)
- **[HOCH, BESTÄTIGT]** G3-Fairness-Klausel + G2-Eingabebasiitheit (`berichtstexte.json`) fehlen am Tür-A/B-Ergebnis und im Preis-Check. Fix: dort wörtlich rendern.
- **[MITTEL, BESTÄTIGT]** `PreisCheck.tsx` zeigt `price.begruendung` (P1–P4) nicht. Fix: Spur analog Wizard rendern.
- **[MITTEL, BESTÄTIGT]** `bedarf.ts` Begründung zeigt Roh-Token „F1 = ja" statt Wortlaut. Fix: Frage-/Optionstext aus `bedarf.json` auflösen.
- **[MITTEL, BESTÄTIGT]** `Wizard.tsx` Transparenzlücke selbstformuliert. Fix: `berichtstexte.weiss_nicht_befund` wörtlich.
- **[MITTEL, BESTÄTIGT]** `BedarfWizard.tsx` Faktor-Visual ohne sichtbare Quelle. Fix: `faktor_visual.quelle` rendern.

### A3 · Standard-Treue / G6 (P1)
- **[MITTEL, BESTÄTIGT]** Betriebsfaktor & Kurs sind laut JSON `editierbar`, in der UI read-only. Fix: editierbar machen (Engine unterstützt Overrides), `erklaerung` zeigen.
- **[MITTEL, BESTÄTIGT]** TCO-Zeitpuffer (T5) fällt im Preis-Check still weg. Fix: Laufzeitfeld oder sichtbarer Befund.
- **[NIEDRIG, BESTÄTIGT]** Standard-Version „1.0" und „/12" im Code hartkodiert. Fix: aus den Daten ableiten.

### A4 · Usability (P1)
- **[HOCH, BESTÄTIGT]** State-Verlust: „Zurück zum Score" / Reload löscht alle Antworten; kein Zwischenspeichern. Fix: State in `Assessment` heben + versioniertes `localStorage`.
- **[HOCH, BESTÄTIGT]** Quellen als JSON-Dateinamen („bedarf.json F7"). Fix: Klartext-Quellen aus `quelle`-Feldern.
- **[HOCH, BESTÄTIGT]** Kontrast `text_3`/helle Ampeltöne unter WCAG-AA. Fix: Werte in `branding.json` anheben.
- **[MITTEL, BESTÄTIGT]** Fehlende „Mehr dazu" (Washing-Faktor-Schwellen, Betriebsfaktor-`erklaerung`); Fokus/`aria-live` beim Fragenwechsel; F7-Input ohne Label; mobile Live-Vorschau unter der Falte; Ergebnis-Chip immer Akzentblau statt Ampel; Consent-Checkbox folgenlos (bis M5 ausblenden); `prefers-reduced-motion` bei JS-Scroll.

### A5 · Security & Robustheit (P1)
- **[HOCH, BESTÄTIGT-Sink]** `design.ts`/`layout.tsx` — `branding.json` ungefiltert in `<style dangerouslySetInnerHTML>`. Fix: Token beim Laden strikt validieren (fail-closed) — vor Directus/White-Label.
- **[HOCH, BESTÄTIGT-fehlend]** Keine CSP/Security-Header. Fix: `headers()` mit Script-Hash des statischen Init-Scripts, `frame-ancestors 'none'`, `object-src 'none'` etc.
- **[MITTEL, BESTÄTIGT]** `content-loader.ts` verschluckt kaputte Minis still. Fix: nur `ENOENT` fangen, Parse-Fehler durchreichen.
- **[MITTEL, BESTÄTIGT]** `force-dynamic` + `readFileSync` je Request. Fix: In-Memory-Cache (mtime/TTL).
- **[NIEDRIG]** `npm audit`/Dependabot in CI; `.claude/settings.local.json` in `.gitignore`.

---

## Topf B — Standard-/Konzept-Fragen (gemeldet; Entscheidung Gottfried)

- **B1** Ampel-Grenze: Konzept „3–10" vs. JSON „2–10"; Inklusiv/Exklusiv bei genau 2,0/10,0. (bestimmt A1)
- **B2** Bedarf-Regelreihenfolge: R01/R02 (Daten/Prozess) maskieren R03 (irreversibles Risiko). Risiko zuerst prüfen oder alle N0-Gründe additiv?
- **B3** Bedarf & „weiß nicht": F2=immer + F3=weiß-nicht → Fallback N4 (Aufwärtsdrift durch Unwissen, widerspricht G5). Fallback-Ziel überdenken?
- **B4** F2=immer + F3=unvorhersehbar → N5; widersprüchliche Eingabe wird nicht markiert.
- **B5** F7 (Volumen) wird von keiner Bedarfs-Regel verwendet — beabsichtigt?
- **B6** „Weiß ich nicht" fehlt in `bedarf.json` F1/F5/F6/F7 und mini-ai-act/bafa, obwohl die Regel es überall fordert.
- **B7** P2(a)-Marktanker (`preise-support.json`) und M3-Sonderfall (`achsen.json` `kostenklasse_wenn_muster_M3`) noch nicht umgesetzt — Scope?
- **B8** mini-ai-act: imperative Pflichtformulierungen — Kanzlei-Review, ggf. konditional.

## Topf C — Fehlende JSON-Felder (gemeldet; Vorschlag zur Freigabe)

- **C1** Ampel-Kurzlabel in `preislogik.json` (`washing_faktor.ampel[].label`) und `schnelltest.json` (`auswertung.ergebnisse[].label`) — aktuell im Code erfunden.
- **C2** Mini-`untertitel` und `zeitangabe`-Nutzung; BAFA-Fristdatum aus `berichtstexte.bafa_vermerk.gueltig_bis`.
- **C3** Optional: `nachkommastellen` für Preis-/Faktor-Anzeige in `preislogik.json`.

---

## Status

Befunde erhoben und verifiziert. Umsetzung Topf A steht unter Freigabe-Vorbehalt;
Topf B/C sind an Gottfried gemeldet. Auflösungen (Fix-Commits, Entscheidungen)
werden hier nachgetragen, sobald umgesetzt.
